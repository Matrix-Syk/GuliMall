spring:
  cloud:
    gateway:
      routes:
      - id: test_route
        # 访问地址
        uri: https://www.baidu.com
        # 参数有url=baidu就访问上面的uri
        predicates:
          - Query=url,baidu
      - id: qq_route
        uri: https://www.qq.com
        predicates:
          - Query=url,qq